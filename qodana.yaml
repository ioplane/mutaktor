version: "1.0"

profile:
  name: qodana.recommended

exclude:
  - name: All
    paths:
      - build-logic
      - "**/build/**"
      - "**/test/**"
      - "**/functionalTest/**"
      - docs
      - deployment

linter: jetbrains/qodana-jvm-community:2025.3
